<% @menus.each do |menu| %> 
  <% if policy(Menu.new).edit? %>
  </br></br>
    <%= link_to "#{menu.name}", edit_menu_path(menu), class: 'btn btn-success' %>
  </br></br>
  <% end %>
<%end%>




<!-- scope :by_day ->{ |date| where(date: date)} -->