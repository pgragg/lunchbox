<tr> 
  <th><h3> <%= title %></h3></th>
  <%i = 0 %> 
  <% number_of_columns.times do %> <!-- Once for each lunch choice. --> 
    <!-- i stands for lunch number.  -->
      <td><%=LunchChoice.column_totals(i, date, menu_id, grade_range, role)%></th>
    <%i += 1 %>
  <% end %> 
  <td><%= LunchChoice.RH_sum(date, menu_id, grade_range, role, number_of_columns)%></td> 
</tr>




