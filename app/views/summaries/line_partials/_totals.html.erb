<tr> 
<th><h3> <%= title %></h3></th>
<%i = 0 %> 
<% number_of_columns.times do %> <!-- Once for each lunch choice. --> 
  <!-- i stands for lunch number.  -->
    <td><%=LunchChoice.totals(i, date, menu_id, grade_range, role)%></th>
  <%i += 1 %>
<% end %> 
<td><%= LunchChoice.sum(number_of_columns, date, menu_id, grade_range, role){|x,y,a,b,c|LunchChoice.totals(x, y, a, b, c) }%></td> 
</tr>




